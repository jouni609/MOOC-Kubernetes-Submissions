apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: exercises
type: Opaque
stringData:
  # Copy to secret.yaml and replace with your actual database password
  POSTGRES_PASSWORD: "YOUR_POSTGRES_PASSWORD_HERE"
