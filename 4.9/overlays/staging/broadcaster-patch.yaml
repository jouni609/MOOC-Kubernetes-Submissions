apiVersion: apps/v1
kind: Deployment
metadata:
  name: broadcaster-dep
spec:
  template:
    spec:
      containers:
        - name: broadcaster
          env:
            # Staging: log messages only; do not forward to external services.
            - name: WEBHOOK_URL
              value: ""
