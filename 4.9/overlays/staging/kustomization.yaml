apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: broadcaster-patch.yaml
  # Staging: database is not backed up
  - path: delete-backup-cronjob.yaml

# Staging deploys on every commit to main (ArgoCD targetRevision: HEAD).
