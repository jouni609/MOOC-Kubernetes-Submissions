apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base
  - namespace.yaml

# Production deploys from tagged commits (ArgoCD targetRevision: release tag).
# Keeps backup CronJob and broadcaster WEBHOOK_URL from base.
