apiVersion: v1
kind: Service
metadata:
  name: landingpage-svc
spec:
  type: NodePort
  selector:
    app: landingpage
  ports:
    - name: http
      protocol: TCP
      port: 1234
      targetPort: 3000
      nodePort: 30080
